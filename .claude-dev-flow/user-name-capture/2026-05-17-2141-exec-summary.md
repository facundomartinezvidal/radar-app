# Execution summary — user-name-capture

**Date:** 2026-05-17
**Status:** Complete (PR pending)
**Branch:** `feat/user-name-capture` (single branch — sub-branch
convention from `/define` skipped because `feat/<name>` and
`feat/<name>/<sub>` cannot coexist as git refs)

---

## What shipped

All 9 atomic changes from the feature definition landed as separate
conventional commits on `feat/user-name-capture`:

| Commit          | Subject                                                                                |
| --------------- | -------------------------------------------------------------------------------------- |
| `a8b8911`       | feat(profiles): add first_name + last_name columns                                     |
| `d588135`       | feat(schemas): add profile name schema shared across auth + profile screens           |
| `337a19a`       | feat(ui): add Avatar primitive with hash-color initials fallback                       |
| `13a9612`       | feat(auth): capture first_name + last_name in sign-up form                             |
| `6f8ac6a`       | feat(onboarding): gate authenticated users behind profile-setup screen                |
| `34f493a`       | feat(home): greet user with real first name + mount Avatar primitive                  |
| `2ea0c21`       | feat(profile): add Perfil stack with view + edit-name + sign-out                       |
| `6de649b`       | feat(auth): personalise OTP email greeting with first_name                             |
| (this commit)   | docs: ADR + feature doc + AGENTS update                                                |

---

## Verification

- Tests: 303/303 pass across 28 suites (baseline pre-feature was 267).
- `pnpm typecheck` clean.
- `pnpm lint` clean.
- `pnpm format:check` clean for all files touched by this branch.
- DB: migration `add_first_last_name_to_profiles` applied via Supabase
  MCP. New columns + triggers verified by direct SQL queries.

## Decisions logged

- Single branch instead of sub-branches per atomic — see Context note
  above. Each atomic is still an isolated conventional commit.
- Avatar palette: 8 hues from DS tokens (blues + ambers + emeralds).
  Explicitly rejected purple/pink per DS rules.
- `display_name` becomes trigger-computed; clients never write it.
- Onboarding gate uses JWT `user_metadata` rather than a fetch to
  `profiles` — store rehydrates synchronously from the auth listener.
- Perfil is a STACK screen, not a tab. Avatar in home header is the
  primary affordance (tap → push Perfil). Tab bar stays at 4 items.

## Manual follow-ups (not in this PR)

1. Paste the updated `confirm-signup.html` into Supabase Dashboard →
   Authentication → Email Templates → Confirm signup. The template
   file is the source of truth; the dashboard is the delivery channel.
2. When the `feat/user-flows-docs` branch lands on `main`, add the
   missing `HU-02-registro-con-nombre.md` spec mirroring the prose in
   the ADR. Skipped here because the `docs/user-flows/` directory
   doesn't exist on `main` yet.

## Out of scope (per definition doc)

- Avatar image upload — `profiles.avatar_url` column exists, no UI yet.
- Auto-split full name into first/last.
- Multi-language input — Latin set only via regex.
- Push notifications with first_name — hook ready, payload deferred to
  groups feature.
