# Execution Summary: expense-line-items (HU-18)

Date: 2026-06-03
Feature branch: `feat/expense-line-items` → `main`

## Resultado

HU-18 "Items detallados" implementada end-to-end en 6 cambios atómicos (PRs #5–#9 + tests-docs):

1. **DB** (#5): `expense_items` + RPCs transaccionales + `supabase/migrations/` versionado nuevo en repo (baseline 2 + nuevas 2, aplicadas vía MCP, historia remota sincronizada). Advisors: cero warnings nuevos.
2. **Edge function** (#6): `extract-receipt` v2 deployed — extrae `items[]` (cap 50, normalización defensiva, prompt español extendido).
3. **Data layer** (#7): zod schemas (OCR camelCase + form snake_case), `mapOcrItems`, repo ruteado por RPCs, hooks `ExpenseWithItems`.
4. **Form UI** (#8): `ExpenseItemsField` colapsable con field array, recompute qty×precio, warning mismatch no bloqueante, DS-compliant.
5. **Screens** (#9): wiring + fix real (OCR items-only caía en rama "sin datos").
6. **Docs**: feature doc, ADR, HU-18 user-flow (+ mirror Obsidian), AGENTS.md actualizado (schema, práctica migraciones versionadas, práctica AGENTS.md-siempre-al-día, shipped/pending).

## Métricas

- Tests: 401 → 468 (+67), 39 suites, 100% pass
- Gates verdes en cada atomic (format/lint/typecheck/test)
- 2 prácticas nuevas codificadas en AGENTS.md

## Desviaciones documentadas

- Atomic branches con guión (`feat/expense-line-items-<x>`) — git ref conflict con `feat/x/y`
- `.mcp.json` → gitignore/prettierignore (config local de agente)
- Item ids rotan en update (delete-all + reinsert) — limitación conocida, pendiente #10 en AGENTS.md

## Pendiente post-merge (humano)

- Merge del PR final a `main`
- Smoke manual en device: scan ticket real → items → guardar → editar
- Advisors pre-existentes (SECURITY DEFINER triggers + leaked password) — AGENTS.md §10.9
