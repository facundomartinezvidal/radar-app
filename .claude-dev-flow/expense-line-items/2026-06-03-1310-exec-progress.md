# Execution Progress: expense-line-items (HU-18)

Date: 2026-06-03
Feature branch: `feat/expense-line-items` (base: `main`)

> Nota: atomics usan sufijo con guión (`feat/expense-line-items-<atomic>`) —
> git no permite `feat/x/y` cuando `feat/x` existe como branch.

## Atomic changes

| # | Branch | PR | Estado | Notas |
|---|--------|----|--------|-------|
| 1 | `feat/expense-line-items-schema` | #5 | ✅ merged | 4 archivos en `supabase/migrations/` (2 baseline + 2 nuevas), aplicadas vía MCP (versiones 20260603152601/20260603152639), advisors sin warnings nuevos, types regenerados. `.mcp.json` → gitignore/prettierignore |
| 2 | `feat/expense-line-items-edge-function` | #6 | ✅ merged | `normaliseItems` + prompt items + README; **deployed v2** al remoto |
| 3 | `feat/expense-line-items-schemas-repo` | #7 | ✅ merged | zod schemas, mapOcrItems, repo RPC paths, hooks; +42 tests (445) |
| 4 | `feat/expense-line-items-form-ui` | #8 | ✅ merged | ExpenseItemsField + ExpenseForm; +17 tests (462) |
| 5 | `feat/expense-line-items-screens-wiring` | #9 | ✅ merged | Fix real: items-only OCR caía en "No se detectaron datos"; +6 tests (468) |
| 6 | `feat/expense-line-items-tests-docs` | — | 🔄 in progress | AGENTS.md actualizado (schema, prácticas migraciones + AGENTS.md-always, shipped/pending); docs delegados a technical-writer |

## Quality gates

Todos los atomics: `pnpm format:check` ✅ · `lint` ✅ · `typecheck` ✅ · `test` ✅
Tests: 401 → 468 (+67).

## Incidentes / desviaciones

1. **Branch naming**: `feat/x/y` imposible con `feat/x` existente (git ref directory conflict) → guiones. Desviación menor documentada.
2. **`.mcp.json` local** rompía `format:check` → agregado a `.gitignore` + `.prettierignore` (no se puede formatear: archivo de config del agente).
3. **Agente atomic 3 desconectado** (socket) a mitad de trabajo → segundo agente completó repo/hooks/tests sobre el diff parcial verificado.
4. **Advisors pre-existentes** (SECURITY DEFINER trigger functions ejecutables por anon/authenticated + leaked password protection off) → anotados en AGENTS.md §10 pendientes. Cero warnings nuevos de esta feature.

## Verificación remota realizada

- `expense_items`: RLS enabled, 4 policies, 2 RPCs presentes (smoke SQL)
- `list_migrations`: historia remota = archivos locales (4/4)
- Edge function `extract-receipt` v2 ACTIVE
