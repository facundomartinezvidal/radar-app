# Docs plan: HU-16 Custom Categories

Date: 2026-06-07
Status: In progress

## Deliverables

1. `docs/features/custom-categories.md` — feature overview
2. `docs/decisions/2026-06-07-custom-categories-schema.md` — ADR
3. `docs/user-flows/HU-16-categorias-personalizadas.md` — user flow (repo canonical)
4. Mirror user-flow to Obsidian vault
5. Update `docs/user-flows/README.md` index (add HU-16 row)
6. Update `AGENTS.md` (categories row, §10 shipped/pending, test baseline 572)

## Style references

- Feature doc mirrors: `docs/features/expense-line-items.md`
- ADR mirrors: `docs/decisions/2026-06-03-expense-line-items-schema.md`
- User-flow mirrors: `docs/user-flows/HU-18-items-detallados.md`

## Key facts to capture

- Schema: `categories` altered with nullable `user_id`, `updated_at`, two partial unique
  indexes replacing global slug unique, `categories_name_nonempty` check, 4 new RLS policies.
- Data layer: no RPC — single-table CRUD under RLS. `normalizeName` generates slug.
  Postgres 23505 → friendly message.
- OCR: edge fn v3 accepts `categories: string[]` + returns `suggestedNewCategory`. Client
  surfaces CTA only when `category_id === null`.
- UI: curated icon/color pickers, `CategoryForm` live preview, `CategoryCreateSheet`,
  "+ Categoría" chip in picker, Perfil > Categorías screen.
- Known pre-existing issue: system "Hogar" category uses icon `Home` (not a valid
  `lucide-react-native` export; correct name is `House`). Not introduced by HU-16.
- Tests: 472 → 572 baseline.
