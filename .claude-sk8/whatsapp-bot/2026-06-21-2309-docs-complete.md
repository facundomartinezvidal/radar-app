# Docs completion report — WhatsApp Bot (HU-26..29)
Date: 2026-06-21
Status: Complete

## Files created

1. `docs/decisions/2026-06-21-whatsapp-bot-schema.md` — ADR for 4 tables + 9 RPCs, identity bridge rationale (D1), security model, migration file index
2. `docs/features/whatsapp-bot.md` — consolidated feature doc: overview table, full architecture + request flow, all 4 HUs, edge cases table, key files table, Meta deployment steps
3. `docs/user-flows/HU-26-bot-whatsapp.md` — account linking: generate code, redeem outcomes, unlink from app/bot, diagram, acceptance criteria
4. `docs/user-flows/HU-27-bot-registro-gasto.md` — capture: text, audio, image, PDF, confirm/cancel state machine, diagram, acceptance criteria
5. `docs/user-flows/HU-28-bot-consultas.md` — queries: totals vs by-category, period table, acceptance criteria
6. `docs/user-flows/HU-29-bot-recomendaciones.md` — recommendations: payload assembly, generate-insights s2s, insufficient-data guard, diagram

## Files updated

7. `docs/conventions/database.md` — Pattern 1 list appended with 8 whatsapp DEFINER RPCs
8. `AGENTS.md` — §6 DB schema: 1-line row for whatsapp_* tables + RPCs; §9 test baseline 1839→1871; §10 shipped: WhatsApp bot entry; §10 pending: replaced item 6 (removed "Decide WhatsApp Business API integration scope", added "Complete Meta business verification + long-lived token")

## AGENTS.md edits (verbatim)

### §6 DB schema (added line):
`- \`whatsapp_links\`, \`whatsapp_link_codes\`, \`whatsapp_messages\`, \`whatsapp_conversations\` — identity bridge + idempotency + confirmation state for the WhatsApp bot; 9 RPCs (1 INVOKER + 8 Pattern-1 DEFINER \`_for\` variants, revoked from \`authenticated\`) (HU-26..29) → [docs/decisions/2026-06-21-whatsapp-bot-schema.md]`

### §9 quality gates (changed):
`pnpm test           # jest-expo + RNTL (1871 tests, 112 suites baseline)`

### §10 shipped (added line):
`- WhatsApp bot (HU-26..29): account linking (one-time code), text/audio/image/PDF capture with confirm gate, movement queries, recommendations; \`whatsapp-webhook\` edge fn (JWT-less, HMAC-SHA256 auth, async via \`EdgeRuntime.waitUntil\`); 4 tables + 9 RPCs (tests 1839 → 1871) → [docs/features/whatsapp-bot.md], [docs/decisions/2026-06-21-whatsapp-bot-schema.md], [docs/user-flows/HU-26-bot-whatsapp.md]`

### §10 pending (item 6 replaced):
Old: `6. Decide WhatsApp Business API integration scope`
New: `6. Complete Meta WhatsApp business verification to reach more than 5 recipients; replace temporary dashboard token with a long-lived system-user token`

## Inconsistencies found (for PM review)

1. **"Invalid Twilio signature" in feature-def.md** — `/.claude-sk8/whatsapp-bot/2026-06-21-1941-feature-def.md` acceptance criteria line 8 says "Invalid Twilio signature → 403". Provider is Meta (not Twilio); implementation correctly uses `X-Hub-Signature-256`. This is a copy-paste artifact in the feature definition doc only. All code, specs, and final docs are correct. Recommend correcting the feature-def.md for traceability.

2. **Migration comment timestamp mismatch** — `20260622010559_whatsapp_service_rpcs.sql` header comment says "Applied to remote: 20260622010000" but the filename is `20260622010559`. Minor discrepancy; no functional impact.

3. **Suite baseline count** — I updated the test count to 1871 (+32) and suites to 112 (+6) based on the 6 test files added in `whatsapp-webhook/`. The exact suite count should be verified with `pnpm test` before the PR merges.
