# Execution Summary — whatsapp-bot-twilio

Date: 2026-06-22
Branch: `feat/whatsapp-bot-twilio` (off `feat/whatsapp-bot`)
Mode: spec-driven (`/sk8:build`, OpenSpec change `whatsapp-bot-twilio`)

## Outcome
Transport swap Meta WhatsApp Cloud API → Twilio WhatsApp. Schema, RPCs, business
logic, app screen reused unchanged. Two delegated increments + gates.

## Increment 1 — backend transport swap (@sk8:developer)
- `verify.ts` rewritten: `buildTwilioSignatureBase` + `validateTwilioSignature`
  (HMAC-SHA1, Web Crypto, constant-time). `checkHandshake` + Meta HMAC-SHA256 removed.
- `parse.ts` (new): `parseTwilioForm` form → internal `WaMessage`/`WaContact`;
  `MediaUrl` carried in `.id`. `parse.test.ts` (new): text/image/audio/pdf/prefix/null.
- `twilio.ts` (replaces `graph.ts`): `sendText` (Messages API, Basic auth, 15s timeout),
  `fetchMediaBytes` (single Basic-auth GET). `graph.ts` deleted.
- `index.ts` rewritten: form parse → `X-Twilio-Signature` verify (403) → `parseTwilioForm`
  → `handleMessage` → empty TwiML ack. GET handshake removed.
- Import swap `./graph.ts`→`./twilio.ts` in `dispatch.ts`/`capture.ts`/`queries.ts`/`recommendations.ts`.
- `verify.test.ts` rewritten (node-verified vectors). `dispatch.test.ts`/`capture.test.ts` unchanged.

## Increment 2 — app sandbox copy (@sk8:developer)
- `lib/repositories/whatsapp.ts`: `WHATSAPP_BOT_NUMBER`→`+14155238886`; new `WHATSAPP_SANDBOX_JOIN`.
- `profile/whatsapp.tsx`: 4-step instructions incl. Twilio Sandbox `join` phrase.
- Screen test: asserts `join` step renders. 11 tests pass.

## Quality gates
- `pnpm format:check`: changed files clean (pre-existing untracked noise unrelated).
- `pnpm lint`: exit 0.
- `pnpm typecheck`: exit 0.
- `pnpm test`: 1871/1871, 109 suites — baseline unchanged.
- deno tests (`verify.test.ts`, `parse.test.ts`) authored with node-verified HMAC-SHA1
  vectors; not CI-gated (no deno in CI) and deno not installed locally.

## Left for user (needs Twilio creds — tasks 7.1–7.3)
Set edge secrets (`TWILIO_ACCOUNT_SID`/`TWILIO_AUTH_TOKEN`/`TWILIO_WHATSAPP_FROM`),
deploy `whatsapp-webhook`, point Twilio Sandbox inbound webhook at the function URL,
record the `join <keyword>`, then manual end-to-end.

## Docs
`docs/decisions/2026-06-22-whatsapp-twilio-transport.md` written. Feature-doc provider
note + AGENTS.md deferred to branch merge.
